node_modules
ventures/lexikit/

# Environment files — never commit real credentials
.env
.env.*
!.env.example
!.env.*.example
!.env.local.example

# Vercel (no longer used)
vercel.json
.vercel/

# Python bytecode (pinterest-bot and other scripts)
__pycache__/
*.pyc
*.pyo
pinterest-bot/__pycache__/

# brain-v2 / org scratch dirs — NEVER commit org drafts into the live site repo
# (these caused the 800-commit divergence; keep them out so git add -A can't sweep them)
ventures/drive-credit/amazon/
ventures/drive-credit/hr/
