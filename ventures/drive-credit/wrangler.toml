name = "drive-credit"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# @cloudflare/next-on-pages outputs to .vercel/output/static (Cloudflare Pages convention)
pages_build_output_dir = ".vercel/output/static"
